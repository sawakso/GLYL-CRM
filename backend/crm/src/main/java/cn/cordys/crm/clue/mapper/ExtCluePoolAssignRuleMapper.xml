<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cordys.crm.clue.mapper.ExtCluePoolAssignRuleMapper">

    <resultMap id="assignRuleMap" type="cn.cordys.crm.clue.dto.CluePoolAssignRuleDTO">
        <id property="id" column="id"/>
        <result property="poolId" column="pool_id"/>
        <result property="ruleName" column="rule_name"/>
        <result property="conditions" column="conditions"/>
        <result property="assignType" column="assign_type"/>
        <result property="targetUserIds" column="target_user_ids"/>
        <result property="currentIndex" column="current_index"/>
        <result property="pos" column="pos"/>
        <result property="enable" column="enable"/>
        <result property="organizationId" column="organization_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createUser" column="create_user"/>
        <result property="updateUser" column="update_user"/>
    </resultMap>

    <insert id="batchInsert">
        insert into clue_pool_assign_rule
        (id, pool_id, rule_name, conditions, assign_type, target_user_ids, current_index, pos, enable,
         organization_id, create_time, update_time, create_user, update_user)
        values
        <foreach collection="rules" item="rule" separator=",">
            (#{rule.id}, #{rule.poolId}, #{rule.ruleName}, #{rule.conditions}, #{rule.assignType},
             #{rule.targetUserIds}, #{rule.currentIndex}, #{rule.pos}, #{rule.enable},
             #{rule.organizationId}, #{rule.createTime}, #{rule.updateTime}, #{rule.createUser}, #{rule.updateUser})
        </foreach>
    </insert>

    <delete id="deleteByPoolId">
        delete from clue_pool_assign_rule
        where pool_id = #{poolId} and organization_id = #{organizationId}
    </delete>

    <select id="selectByPoolId" resultMap="assignRuleMap">
        select * from clue_pool_assign_rule where pool_id = #{poolId} order by pos asc, create_time asc
    </select>

    <select id="selectByPoolIds" resultMap="assignRuleMap">
        select * from clue_pool_assign_rule where pool_id in
        <foreach collection="poolIds" item="poolId" open="(" close=")" separator=",">
            #{poolId}
        </foreach>
        order by pool_id asc, pos asc, create_time asc
    </select>

    <update id="compareAndSetCurrentIndex">
        update clue_pool_assign_rule
        set current_index = #{currentIndex}, update_user = #{updateUser}, update_time = #{updateTime}
        where id = #{id}
          and organization_id = #{organizationId}
          and enable = true
          and (
            (#{expectedIndex} is null and current_index is null)
            or current_index = #{expectedIndex}
          )
    </update>

    <select id="selectEnabledByPoolId" resultType="cn.cordys.crm.clue.domain.CluePoolAssignRule">
        select * from clue_pool_assign_rule
        where pool_id = #{poolId}
          and organization_id = #{organizationId}
          and enable = true
        order by pos asc, create_time asc
    </select>

</mapper>
